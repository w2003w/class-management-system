{
  "name": "class-management-system",
  "version": "1.0.0",
  "description": "班级管理系统 - Cloudflare Workers Sites",
  "scripts": {
    "build": "npm install",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.20.0"
  }
}